[pytest]
markers =
    integration: corre el pipeline completo con LLM/RAG/MCP reales (tests/test_scenarios.py). Gasta cuota real de API y tarda minutos por escenario; se excluye por default.
addopts = -m "not integration"
