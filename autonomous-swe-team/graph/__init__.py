"""Paquete del grafo de orquestación (LangGraph).

Responsabilidad dentro del sistema:
    Contiene la definición del estado compartido (state.py), los nodos que
    envuelven a cada agente (nodes.py), las conditional edges de enrutamiento
    según el veredicto del Reviewer (edges.py) y la construcción/compilación
    del workflow completo (workflow.py).
"""
